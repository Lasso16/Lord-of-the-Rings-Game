package org.tokioschool.view;

import javafx.geometry.Orientation;
import javafx.scene.control.Separator;

public class CenterPane extends Separator {
    public CenterPane() {
        setOrientation(Orientation.VERTICAL);
    }
}
